# Runtime (dependencias principales; ver pyproject.toml [project] dependencies)
openai>=1.40.0        # OpenAI-compatible client (OpenRouter, etc.)
pydantic>=2.0         # structured LLM output parsing
aiohttp>=3.9          # async http for optional fetches
cryptography>=42.0    # ed25519 (default del modo estricto del provenance)

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23

# ---------------------------------------------------------------------------
# Extras (opcionales, por transporte/capa) — ver pyproject.toml [project.optional-dependencies]
#   pip install delm[nostr]  -> relay Nostr de red (capa 4)
#   pip install delm[quic]   -> QUIC entre hosts (capa 3)
#   pip install delm[mdns]   -> mDNS discovery (capa 4)
#   pip install delm[web]    -> api_server.py (FastAPI :8099)
#   pip install delm[all]    -> los cuatro anteriores
# ---------------------------------------------------------------------------

# [nostr] relay Nostr de red (capa 4)
websockets>=12.0

# [quic] QUIC entre hosts (capa 3)
aioquic>=1.0

# [mdns] mDNS discovery (capa 4)
aiozeroconf>=0.1.8

# [web] API + estático (api_server.py)
fastapi>=0.110
uvicorn>=0.27
